# mealprep-machine
 A website that generates you a mealprep schedule

## Development

Data persistence is handled with a postgres container. 

To run, use `docker compose up -d`
To stop, use `docker compose down`
To stop and delete the database to start fresh, use `docker compose down --volumes`
